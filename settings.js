// ----- Settings -----
const quoteContentMaxHeight = 100; // Maximum allowed height for quotes before they get collapsed and added Collapse/Expand button (in pixels).
const quoteCollapserButtonBackground = "#242424"; // Collapse/Expand button background color (in hexadecimal).
const quoteCollapserButtonPadding = 10; // Collapse/Expand button padding (in pixels).
const quoteCollapserButtonFontSize = 17; // Collapse/Expand button text font size (in pixels).
const quoteCollapserButtonCollapsedText = "↓ Expand ↓"; // Collapse/Expand button text when the quote is collapsed.
const quoteCollapserButtonExpandedText = "↑ Collapse ↑"; // Collapse/Expand button text when the quote is expanded.
const removeSidebar = false; // If you want to remove sidebar, set this to true, otherwise set it to false;
// --------------------